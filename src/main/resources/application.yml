logging:
  level:
    org:
      mongodb:
        driver:
          cluster: INFO
    de:
      flapdoodle:
          embed: INFO

spring:
  data:
    mongodb:
      uri: 'mongodb+srv://${db_username}:${db_password}@cluster0.lmzw1ax.mongodb.net/product'
      database: product
      port: 27017